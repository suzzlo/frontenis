<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
  <tr>
    <td>
      <div class="field">
          <%= f.collection_select :championship_id, Championship.order(start_date: :desc), :id, :name, {} %>
      </div>
      <br>
      <div class="field">
        <%= f.label :played %><br />
        <%= f.datetime_select :played %>
      </div>
    </td>
    <td>
<table>
  <tr>
    <td>TEAM 1</td>
    <td>TEAM 2</td>
  </tr>
  <tr>
    <td>
      <div class="field">
        <%= f.number_field :t1_points %>
      </div>
      <div class="field">
        <%= f.collection_select :t1_p1, User.order(:name), :id, :name, { :prompt => 'Select player'} %>
      </div>
      <div class="field">
        <%= f.collection_select :t1_p2, User.order(:name), :id, :name, { :prompt => 'Select player'} %>
      </div>
    </td>
    <td>
      <div class="field">
        <%= f.number_field :t2_points %>
      </div>
      <div class="field">
        <%= f.collection_select :t2_p1, User.order(:name), :id, :name, { :prompt => 'Select player'} %>
      </div>
      <div class="field">
        <%= f.collection_select :t2_p2, User.order(:name), :id, :name, { :prompt => 'Select player'} %>
      </div>
    </td>
  </tr>
</table>
    </td>
  </tr>
</table>









  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
